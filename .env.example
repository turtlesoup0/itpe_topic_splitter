# ─── LLM Provider 설정 ─────────────────────────────────────────
# "anthropic" (기본, Haiku API) | "mlx" (로컬 MLX-LM 서버)
LLM_PROVIDER=anthropic

# Anthropic Haiku API 키 (LLM_PROVIDER=anthropic일 때)
ANTHROPIC_API_KEY=

# MLX 로컬 서버 설정 (LLM_PROVIDER=mlx일 때)
MLX_URL=http://127.0.0.1:8090
MLX_MODEL=Jiunsong/supergemma4-26b-uncensored-mlx-4bit-v2

# ─── API 토큰 (선택) ───────────────────────────────────────────────
# 설정 시 /api/split 호출에 Authorization: Bearer <token> 필수
# 미설정 시 공개 모드 (rate limit만 적용)
# 생성: python3 -c 'import secrets; print(secrets.token_urlsafe(32))'
ITPE_API_TOKEN=
